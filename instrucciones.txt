$npm install -g pnpm  # para crear el entorno del proyecto: 

if Error:
    open powershell as ADMIN:
    $ Set-ExecutionPolicy RemoteSigned 
    $ Y # para activar permisos 

$pnpm add @modelcontextprotocol/sdk
$pnpm add zod

$echo import { McpServer } from "@modelcontextprotocol/sdk/server/mcp.js";
echo import { StdioServerTransport } from "@modelcontextprotocol/sdk/server/stdio.js";
echo import {z} from "zod";

//create server 
const server = new McpServer({
    name: "Demo Server",
    version: "1.0.0",
})

//crear herramientas 
//las herramientas permiten al LLM realizar acciones a través del API de la herramienta

server.tool(
'fetch-wearther',
'tool to fetch weather information for a given city',
{
    city: z.string().describe("City name"),
}
    async({city}) => {
        return {
        content: [
            {
                type: "text",
                text: `The weather in ${city} is sunny with a temperature of 25 degrees Celsius.`
            }
        ]
    }
)

//3. escuchar las conexiones del cliente
const transport =  new StdioServerTransport()
await server.connect(transport)
>main.ts

//4 . configuración de inspector: 
$ npx -y @modelcontextprotocol/inspector npx -y tsx main.ts 
<Navegador> 
http://127.0.0.1:6274/#resources 